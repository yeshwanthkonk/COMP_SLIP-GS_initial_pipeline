name: Ground Station Automation Pipeline

on:
  push:
    branches: [ dev,  initial-pipeline ] # need to remove the test branches: initial-pipeline
  # pull_request:
  #   branches: [ dev ]

jobs:
  run-tests-publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # build test image
    - name: Build test image
      run: docker build -f Dockerfile.test -t test-image .

    # run tests
    - name: Run tests in container
      run: docker run --rm test-image

    # build image and push if tests pass
    - name: Build and push production image
      if: success()
      run: |
        IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/comp_slip_gs
        VERSION=1.0.${{ github.run_number }}
        docker build -f Dockerfile -t $IMAGE_NAME:$VERSION .
        docker push $IMAGE_NAME:$VERSION
